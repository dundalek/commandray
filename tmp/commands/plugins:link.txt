Usage: heroku plugins:link [PATH]

Links a local plugin into CLI

Links a local plugin into CLI.
	This is useful when developing plugins locally.
	It simply symlinks the specified path into the plugins directory
	and parses the plugin.

  Example:
	$ heroku plugins:link .

